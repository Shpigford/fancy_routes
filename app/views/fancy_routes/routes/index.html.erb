<pre style="font-family: monospace;">
  <% @sented_more organized_elormize_format = @routes.each_with_object([]) do |route, array| %>
    <% array << { "#{route.verb} #{route.path.spec}".ljust(30) => route.name } %>
  <% end %>
  <%= organized_format.inspect %>
</pre>